define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  
  var TiposLugaresModel = Backbone.Model.extend({
	  
	  defaults: {
	    
	  }
	  
  });

  return TiposLugaresModel;

});
